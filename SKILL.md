# 🎬 视频制作技能 (Video Maker Skill)

> 让 AI 能够基于 Remotion 快速制作视频的完整技能包

## 核心能力

✨ **主动提示词补充** - AI 会自动学习优秀提示词模式，补充用户输入的内容
🎨 **视觉细节优化** - 自动补充美学、配色、字体、动画等细节
⚡ **技术规格完整化** - 补充分辨率、帧率、时间码、过渡效果等技术参数
🔧 **最佳实践应用** - 遵循 Remotion 官方提示词库的最佳实践

## 触发词

当用户通过以下方式发起视频制作请求时触发：
- "制作视频"
- "做视频"
- "生成视频"
- "帮我做个视频"
- "做个视频"
- "做一个 [主题] 视频"

## 前置要求

### 🎯 提示词增强指南（自动应用）

AI 在接收用户请求时会**自动参考以下优秀模式**来补充和优化提示词：

#### 模式一：详细的视觉规格声明
```
原始输入：做个CEO介绍视频
↓ AI补充后：
分辨率: 1920x1080 | 帧率: 30fps
美学风格: 企业科技范 + 现代简约
主色调: 品牌绿 (#76B900) | 背景: 干净白色 + 微妙纹理
```

#### 模式二：分层的内容描述
```
原始输入：加上文字动画和背景粒子
↓ AI补充后：
第一层（背景）: 纹理化白色背景 + 径向渐变
第二层（主体）: 巨大的标题文字，缩放进场（3x → 1x）
第三层（配件）: 旋转的科技环 + 下落的数据流粒子
第四层（效果）: 竖向扫描线 + 角括号动画 + 浮动几何体
```

#### 模式三：精确的技术参数
```
原始输入：加点动画效果
↓ AI补充后：
- 进场动画: spring() 物理弹簧 + 连续浮动运动
- 特殊效果: 数字故障抖动 (skew/hue-rotate)
- 过渡效果: 玻璃态 UI 面板从左边滑入 (-600px)
- 计时: 第40帧后引入后续元素
- 字体: 等宽字体 + Knewave 艺术字体混搭
```

#### 模式四：具体的库函数建议
```
原始输入：让动画流畅
↓ AI补充后：
✅ 使用 spring() 实现物理感弹跳
✅ 使用 interpolate() 实现精确的时间线动画
✅ 使用 rough.js 绘制手绘风格高光笔效果
✅ 检查现有 lockfile，保持包管理器一致
```

#### 模式五：用户输入补完清单
当用户请求缺少以下内容时，**AI主动补充**：

| 用户提到 | AI 自动补充 | 示例 |
|---------|-----------|------|
| 只提了颜色 | 补充字体、排版、间距 | "蓝色" → "蓝色配白色无衬线字体，1.5倍行距" |
| 只提了结构 | 补充视觉效果、过渡 | "标题+列表" → "加入渐变进场、阴影深度、光晕效果" |
| 只提了内容 | 补充时间码、帧率、转速 | "旋转环" → "30fps, 2秒完整旋转，360deg" |
| 只提了风格 | 补充具体的色值、字体 | "科技感" → "暗绿 #1a3a1a, Courier New, 霓虹绿光晕" |
| 只提了动画 | 补充进场方向、easing、时长 | "缩放" → "从0.3x缓入弹出到1x, 600ms, easeOut" |

**关键原则**：
- ✅ 参考 https://www.remotion.dev/prompts 的优秀提示词
- ✅ 提示词要具体，不能含糊 (从 "加动画" → "使用 spring() 从 -600px 滑入")
- ✅ 技术参数必须完整 (分辨率、帧率、时长、色值代码)
- ✅ 保留用户的核心需求，补充而非改变

---

### 1. 确认 Remotion 项目位置
项目根目录：`./remotion-video-project`

相对路径示例：
- Windows: `remotion-video-project\`
- Mac/Linux: `remotion-video-project/`

### 2. 了解可用的视频组件

| 组件名称 | 用途 | 特点 |
|---------|------|------|
| `TikTokCaption` | 抖音字幕风格 | 逐字淡入 + 底部标签动画 |
| `VideoTemplate` | 通用视频模板 | 支持多种动画效果、可配置标题/副标题/标签/风格 |

### 3. 确认视频配置

在开始制作前，必须向用户确认以下信息：

| 信息项 | 说明 | 追问示例 |
|-------|------|---------|
| **主题/标题** | 视频要表达什么？ | "视频主题是什么？" |
| **时长** | 需要多长？（秒） | "需要几秒？10秒还是30秒？" |
| **字幕内容** | 屏幕上显示什么文字？ | "字幕/文字内容是什么？" |
| **画面风格** | 动态效果还是静态？ | "要什么风格？文字动画？" |
| **背景** | 纯色/图片/视频？ | "背景用纯色还是图片？" |
| **尺寸** | 竖屏/横屏？ | "竖屏抖音还是横屏？" |
| **动画效果** | 是否需要炫彩动画？ | "要加背景动画吗？粒子效果、光晕等" |
| **背景音乐** | 需要添加背景音乐吗？ | "要加背景音乐吗？我可以帮你配上" |

## 制作流程

### 第一阶段：需求分析

```
用户: "制作视频 [描述]"

AI 行为:
1. 提取描述中的关键信息
2. 列出已理解和未理解的部分
3. 向用户确认未明确的信息
4. 等待用户回复后再继续
```

### 第二阶段：代码生成

根据确认的配置，更新 `src/VideoConfig.ts` 中的配置：

```typescript
export const videoConfig = {
  title: '确认后的标题',
  subtitle: '确认后的副标题',
  duration: 确认的时长,
  backgroundColor: '背景色',
  textColor: '文字色',
  accentColor: '强调色',
  // ...
};
```

### 第三阶段：渲染输出

```bash
cd ./remotion-video-project
npm run build
```

### 第四阶段：交付

```
✅ 视频制作完成！

📁 文件位置: ./remotion-video-project/out/[视频文件].mp4
⏱️ 时长: [X]秒
📐 分辨率: 1080x1920 (竖屏)
🎬 帧率: 30fps

💡 如需导出到桌面，请将 ./remotion-video-project/out/ 文件夹中的视频复制到桌面。

如需调整，请告诉我具体要改什么。
```

## 修改流程

用户说"修改"、"调整"、"换个风格"时：
1. 理解修改需求
2. 更新对应的配置值
3. 重新渲染
4. 重新交付

## 注意事项

1. **必须确认清楚再制作** - 不确定的必须问清楚，不能凭空编造
2. **复杂视频分步做** - 太复杂的先做基础版，再叠加功能
3. **保持配置与代码同步** - 修改后检查 VideoConfig.ts

## 快速参考

### 项目结构
```
remotion-video-project/
├── src/
│   ├── index.ts           # 入口
│   ├── Root.tsx           # 组件注册
│   ├── TikTokCaption.tsx  # 抖音字幕组件
│   ├── VideoTemplate.tsx  # 通用模板组件
│   └── VideoConfig.ts     # ⭐ 视频配置（主要修改这里）
├── out/                   # 渲染输出目录
├── package.json
└── tsconfig.json
```

### 启动开发预览
```bash
cd ./remotion-video-project
npm run dev
# 访问 http://localhost:3000
```

### 渲染视频
```bash
cd ./remotion-video-project
npm run build
# 输出到 out/ 目录
```

### 修改视频内容的步骤

1. 打开 `src/VideoConfig.ts`
2. 修改配置值（标题、颜色等）
3. 运行 `npm run build` 重新渲染
4. 在 `out/` 目录找到新视频

### 添加背景音乐的步骤

1. 将音乐文件放在 `src/` 目录（如 `src/bg-music.mp3`）
2. 打开 `src/VideoConfig.ts`，修改：
   ```typescript
   enableAudio: true,            // 启用音乐
   audioPath: './bg-music.mp3',  // 指向你的文件
   audioVolume: 0.3,             // 调整音量
   ```
3. 运行 `npm run build` 重新渲染
4. 新视频将包含背景音乐

## 扩展组件

如果内置组件不满足需求，可以：

1. 在 `src/` 下创建新组件（如 `PromoVideo.tsx`）
2. 在 `Root.tsx` 中注册新 Composition
3. 在 `VideoConfig.ts` 中添加配置
4. 在渲染命令中指定组件名：
   ```bash
   npx remotion render PromoVideo out/promo.mp4
   ```

---

## 技能文件清单

- `SKILL.md` - 本文件，技能定义
- `README.md` - 详细使用文档和示例
- `remotion-video-project/` - 完整的 Remotion 项目模板
- `EXAMPLES.md` - 各种场景的使用示例

---

## 📚 优秀提示词库参考

参考来源：https://www.remotion.dev/prompts

### 类型一：新闻/文章高亮视频
**特点**: OCR集成、精确定位、手绘效果  
**关键技术**:
- 使用 tesseract CLI 进行 OCR 文本识别
- 检查 lockfile，使用正确的包管理器
- rough.js 绘制高光笔效果（在文本后层）
- 3D 变换：缩放 + 旋转（每轴 ~15deg）
- 模糊过渡: 完整模糊→1秒内解除

**补充建议模板**:
```
原始: "突出新闻中的重点词汇"
补充后: 
- 导入高分辨率图片，使用 tesseract OCR 识别文本位置
- 白色全高清背景，文章居中留充分余白
- 高光笔从左向右演进，使用 rough.js 手绘风格
- 记号标签层级: 标记在文本后面
- 5秒内缓慢旋转 (3D, 每轴15°) + 微妙缩放
- 开场: 全局模糊 → 1秒解除
```

### 类型二：企业/CEO 介绍视频  
**特点**: 高能视觉、多层合成、企业美学  
**关键技术**:
- 精确的色值代码和字体名称
- 分层渲染: 背景 → 主文本 → 图片 → 配件 → 效果
- spring() 物理弹簧进场
- 玻璃态 UI 面板 (glassmorphism) 带 clip-path
- 数据流粒子 + 扫描线 + 几何体浮动
- 故障效果: skew + hue-rotate 颤抖

**补充建议模板**:
```
原始: "做个CEO介绍视频，高端一点"
补充后:
- 分辨率: 1920x1080 @ 30fps
- 美学: 赛博朋克 + 企业科技范
- 背景: 干净白色 + 径向噪声纹理
- 主文本: Knewave 字体，NVIDIA 绿 (#76B900)，pop-in 缩放 (3x→1x)
- 人物图片: 右侧爆炸弹簧进场，连续浮动，数字故障 (skew/hue-rotate)
- 配件: 旋转虚线环 + Matrix 数据下落流
- HUD 面板: 左侧滑入 (-600px)，深色半透明绿，剪裁角落
- 粒子: 三角形/六边形浮动，spring() + interpolate()
- 扫描线: 竖向动画 + 角括号动画
```

### 类型三：产品演示/启动视频
**特点**: 专业叙述、多场景切换、动态数据可视化  
**补充建议**:
- 明确场景时间线和过渡方式
- 指定数据可视化的具体形式 (柱状图、折线图、组合图)
- 品牌色的一致性
- 动画节奏配合配乐

### 类型四：三维/高级效果视频
**特点**: Three.js 集成、复杂几何、光照效果  
**关键技术**:
- 确保已安装 three.js 依赖
- 指定 3D 摄像机角度、光源位置
- 动画旋转轴和速度 (deg/second)

---

## 🎨 提示词补充工作流

AI 在每次接收用户请求时的行为：

1. **分析用户输入** → 识别已提供的信息
2. **发现缺失项** → 对标上述"模式五"清单
3. **查询参考库** → 从本文档的优秀提示词库找类似案例
4. **自动补充** → 补充美学、参数、技术细节
5. **确认补充** → 向用户展示完整提示词，争取认可
6. **开始制作** → 根据完整提示词实现

**示例工作流**:
```
用户: "做个旋转的数据图表视频，展示游戏销量排行"

AI分析:
✓ 有内容: 数据可视化、排行榜
✗ 缺失: 分辨率、帧率、美学、动画风格、时长、配色

AI补充:
+ 分辨率: 1920x1080 | 帧率: 30fps | 时长: 10秒
+ 美学: 现代科技暗黑风格
+ 主图: 柱状图 + 排名序号，使用对比色突出 TOP 3
+ 动画: 
  - 开场: 从下向上生长 (0→100%, 800ms)
  - 中间: 排名轮流闪烁高光，旋转摄像机环绕 (360°, 6秒)
  - 结尾: 渐出淡化
+ 颜色: 深蓝 (#0a1628) 背景 + 霓虹彩虹渐变柱子
+ 字体: Roboto Bold 标题 + 等宽字体数值
+ 配乐对齐: 适合 120-130 BPM 的电子音乐

确认后开始制作...
```

---

_小哈米出品 🐹_
_基于 Remotion v4.0.459 + React + TypeScript_  
_学习参考: https://www.remotion.dev/prompts_